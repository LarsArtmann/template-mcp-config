# Environment variables - NEVER commit these
.env
.env.local
.env.*.local

# MCP configuration backups
.mcp.json.backup*
.mcp.json.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed

# Coverage directory used by tools like istanbul
coverage/

# Temporary folders
tmp/
temp/

# Node.js dependencies and lockfiles
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Package manager lockfiles (except bun.lockb which should be committed)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Optional npm cache directory
.npm

# Optional yarn cache
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Optional REPL history
.node_repl_history

# Bun lockfile (regenerated from package.json)
bun.lock
bun.lockb

# Node version file (user preference)
.nvmrc

# Health reports (generated at runtime)
reports/health-*.json

# Optional ESLint cache
.eslintcache

# TypeScript cache
*.tsbuildinfo

# Node.js REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# MCP memory cache files
**/*mcp-memory.json
.claude/settings.local.json
